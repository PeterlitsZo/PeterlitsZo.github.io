#date:init_time=2019/08/07;
为了更改ProgramFilesDir到多个值，我使用regedit展开注册表“HKEY_LOCAL_MACHINE\Software\Microsoft\ Windows\ CurrentVersion”分支，新建了一个REG_MULTI_SZ。
但我还是怀疑这样没有什么用，于是我更改ProgramFilesDir到该目录下的一个子目录处。
子目录含有所有的来源于微软的应用，于是我打算删除原应用。删不了......
之后我把该目录的所有者改到了User处，结果还是不行，又把所有者改到Admin去了（因为我的用户是隶属于这个的），结果又不行......
我又又又又把所有者改到自己的账号（用邮箱做的用户名）......结果还是不行......
现在我想把windows重装到专业版了。
嗯，在网上随便找了个密匙装上了，在计算机管理中把主用户的隶属组更改了，现在应该是拥有真正的所有权限吧。
——————————————
是这样，本来看到有很多文件夹的Program Files文件夹，本来是想将他们分类的。
现在想想，有好多应用都是通过注册表来链接到里面的，我分了类，但是系统软件却用不起了，也没什么用呀。
区别于系统预装的程序，我自己新建了一个叫User Program Files。
在通过全新启动后，在备份文件夹中备份了Program Files和Program Files(x86)和Inter（并删除了原文件夹，他们说这个文件夹没有什么用）
再通过win+i来打开windows更新安全和质量修复。在恢复中的高级启动中，重新启动后再重新启动，按F5进入带网络连接的安全模式。（用来删除Inter）
再新建了MAIN文件夹来存放我的文档和程序。
也规定了User文件夹是用来存放除与程序相关的资源的。
现在再想想，唉，这是真的麻烦欸......但是这个app要安装到program files，但另外一个又要安装到appdate里去......强迫症还是不能忍呀。