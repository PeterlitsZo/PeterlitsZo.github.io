#date:init_time=2019/08/02;
还是想给樱岛弄一个进度条，就用了pyQt，首先要说的是，用方法的形式调用，传入的第一个参数就是self~ 
这就是...takes 1 positional argument but 2 were given的原因了！（至少我的是这样、嘻嘻）
现在为了......欸，为了什么？maye、忘了......
哦、为了调整QProgressBar的最大参数，我过来看看官方文档了~
首先是QProgressBar(parent: QWidget = None)，意思说，（猜的）它的parent必须是QWidget类的，默认为None欸......
来把有点用的方法抄下来：
    1.  isTextVisible()
    2.  maximum()
    3.  setMaximum()
    4.  setTextVisible()
    